

export enum UserStatus{
    ACTIVE = 0,
    INACTIVE = 1,
    DELETED = 2    
};

export enum UserRole{
    EMPLOYEE = "Employee",
    MANAGER = "Manager"
};

export enum UserFormMode{
    ADD = "Add User",
    EDIT = "Edit"
};